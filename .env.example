# Provider API key from OpenRouter; server-only.
OPENROUTER_API_KEY=
# Existing deployment alias for the provider key; OPENROUTER_API_KEY takes precedence.
JEV_ACCESS_TOKEN=
# Separate app password for hosted live calls. Never enter the provider key in the browser.
JEV_APP_PASSWORD=
